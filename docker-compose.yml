services:
  book14:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8082:8082"
#  mongo:
#    image: mongo
#    restart: always
#    environment:
#      - MONGODB_INITDB_ROOT_USERNAME=user
#      - MONGODB_INITDB_ROOT_PASSWORD=pass
#    ports:
#      - "27018:27018"
#    volumes:
#      - mongo_data:/data/db
#    networks:
#      - app-network